export class TinyIconConfiguration {
    public primaryTagName: string
    public resourceUrl: string

    constructor(primaryTagName: string, resourceUrl: string) {
        this.primaryTagName = primaryTagName;
        this.resourceUrl = resourceUrl;
    }
}